#include <stdio.h>

/**
 * main - print the lower alphabets in reverse.
 * Return: Always 0.
 */
int main(void)
{
	char let;

	for (let = 'z'; let >= 'a'; let--)
		purchar(let);

	putchar('\n')

		return (0);
}
